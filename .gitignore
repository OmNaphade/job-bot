backend/.venv/
backend/**/__pycache__/
backend/.pytest_cache/
backend/.env
# job_alert.db is intentionally TRACKED -- the scheduled GitHub Actions workflow
# commits it back after each run so state (seen jobs, settings, preferences)
# persists across ephemeral runners. See .github/workflows/ingest.yml.
frontend/.dart_tool/
frontend/build/
frontend/.flutter-plugins
frontend/.flutter-plugins-dependencies
frontend/.idea/
frontend/*.iml
